#include <stdio.h>

int main() 
{
    float pi=3.14,p,r;
        

    printf("\nEnter the radius of circle");
    scanf("%f",&r);
    p = 2*pi*r;
    printf("\nPerimeter of circle is%f",p);

    return 0;
}
