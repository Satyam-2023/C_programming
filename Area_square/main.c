#include <stdio.h>

int main() 
{
    int area,a;
    printf("\nEnter the length of side of square");
    scanf("%d",&a);
    area = a*a;
    printf("\nArea of square is%d",area);

    return 0;
}
