#include <stdio.h>

int main()
{
    float l,b;
    float area;

    printf("\nEnter the length and breadth");
    scanf("%f%f",&l,&b);
    area = l*b;
    printf("\nArea of rectangle is%f",area);

    return 0;
}
